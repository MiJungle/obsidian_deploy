
```
JSON.stringify({text})
-> stringify text will cause double parsing 
-> prevents app from parsing, as there is double escape 

and text({some object})
-> will make web to show some object object text 
```